// 全局配置
// 真机预览时把 BASE_URL 换成你电脑的局域网 IP，例如 http://192.168.1.10:8000
// 后端启动需要加 --host 0.0.0.0 才能被手机访问
export const BASE_URL = 'http://127.0.0.1:8000'

export const API_PREFIX = '/api/v1'

export const TOKEN_KEY = 'petcost.token'
